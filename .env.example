# API Configuration
# Replace with your actual API key
API_KEY=your-api-key-here

# Optional: Custom API endpoint (defaults to https://api.example.com)
# API_ENDPOINT=https://api.example.com

# Optional: Cache TTL in seconds (defaults to 300 = 5 minutes)
# CACHE_TTL_SECONDS=300

# Optional: Request timeout in milliseconds (defaults to 30000 = 30 seconds)
# TIMEOUT=30000

# Optional: Default limit for list operations (defaults to 100)
# DEFAULT_LIMIT=100

# Optional: Enable debug logging (defaults to false)
# DEBUG=true